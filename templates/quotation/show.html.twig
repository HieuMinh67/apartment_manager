{% extends 'base.html.twig' %}

{% block title %}Quotation{% endblock %}

{% block body %}
    <h1>Quotation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quotation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quotation.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ quotation.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ quotation.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ quotation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quotation_index') }}">back to list</a>

    <a href="{{ path('quotation_edit', {'id': quotation.id}) }}">edit</a>

    {{ include('quotation/_delete_form.html.twig') }}
{% endblock %}
